# Server Configuration
PORT=3001
NODE_ENV=development

# Authentication
# IMPORTANT: Change this to a secure random token in production!
AUTH_TOKEN=change-me-in-production-secure-random-token

# Cache Configuration (in milliseconds)
CACHE_TTL=5000
CACHE_OVERVIEW_TTL=5000
CACHE_JAILS_TTL=5000
CACHE_NGINX_TTL=10000
CACHE_SYSTEM_TTL=30000

# Script Execution
SCRIPT_TIMEOUT=30000

# Sudo Configuration (optional)
# If set, scripts will be executed as this user via sudo
# SUDO_USER=sentinel_user

# Paths
SCRIPTS_DIR=/opt/fail2ban-dashboard/scripts
NGINX_ACCESS_LOG=/var/log/nginx/access.log
FAIL2BAN_CONFIG=/etc/fail2ban

# CORS
# Frontend URL for CORS
CORS_ORIGIN=http://localhost:5173

# Rate Limiting
RATE_LIMIT_WINDOW=60000
RATE_LIMIT_MAX=100
